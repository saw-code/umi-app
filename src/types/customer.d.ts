export interface Customer {
  id?: number;
  name?: string;
  company?: string;
  phone?: string;
  email?: string;
  role?: string;
}

// Интерфейс клиента определяет, как мы будем получать информацию о клиенте от
// бэкэнд.
